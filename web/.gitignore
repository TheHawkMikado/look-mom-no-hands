node_modules/
.next/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Every real env file stays local; the template is committed on purpose so a
# fresh clone can see which variables the site needs.
.env*
!.env.example
